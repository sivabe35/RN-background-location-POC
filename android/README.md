# Android native module and service

Below are the primary Kotlin files used for Android. They live in an Android app project at `android/app/src/main/java/com/sivarn/backgroundlocation/`.

You should merge these into the `MainApplication`/`MainActivity` RN project and add the manifest permissions shown in README.
